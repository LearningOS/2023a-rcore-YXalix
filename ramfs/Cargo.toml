[package]
name = "ramfs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libvfs = { path = "../libvfs" }
spin = "0.7.0"
log = "0.4"


[dev-dependencies]
env_logger = "0.10"